# TraceCarbon dMRV Registry — Python Dependencies
# Install with: pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
ultralytics>=8.3.0              # YOLOv8 tree crown detection
opencv-python-headless>=4.8.0  # Headless OpenCV (no libGL required on server)
pillow>=10.0.0                  # EXIF GPS metadata extraction
earthengine-api>=0.1.380        # Google Earth Engine satellite NDVI
httpx>=0.27.0                   # Async HTTP client (OpenWeatherMap)
python-multipart>=0.0.9         # FastAPI multipart/form-data file uploads
web3>=7.0.0                     # Arc Testnet EVM interaction & on-chain minting
huggingface_hub>=0.23.0         # Auto-download best.pt model weights from HF at startup
